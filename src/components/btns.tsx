import React from 'react';

const Btns = () => {
    return (
        <div className="btns">
            <button className={'active'}>Convert</button>
            <button>Single</button>
        </div>
    );
};

export default Btns;